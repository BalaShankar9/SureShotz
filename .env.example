# Development placeholders only. VITE_* values are visible in the browser.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-public-anon-key
# Existing client code reads these names. Keep them empty until a safe
# development credential strategy is in place; proxy secret keys server-side.
VITE_API_SPORTS_KEY=
VITE_ODDS_API_KEY=
